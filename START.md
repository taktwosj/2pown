# START Pointer

Canonical start procedure is in `docs/START.md`.

- Do not maintain a second start procedure here.
- Use `meta/project_registry.json` only after following `docs/START.md`.
- If this file and `docs/START.md` differ, always follow `docs/START.md`.
